# EmoSync Bug Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix 6 issues regarding onboarding layout scaling, top logo safe area overlapping, back-button logout navigation stack bugs, dashboard card alignment, app renaming, and launcher icon branding.

**Architecture:** Encapsulate page headers in SafeArea widgets, restrict onboarding illustration max heights to 28% of screen height to support smaller devices, modify the navigation flow on login/register to reset the navigator history stack, standardise dashboard cards styling using IntrinsicHeight, update AndroidManifest.xml and Info.plist labels, and generate app icons using flutter_launcher_icons.

**Tech Stack:** Flutter, Dart, flutter_launcher_icons

## Global Constraints
- Target platform names must be "EmoSync"
- Use SafeArea for all top headers
- Keep widget sizing responsive and relative to screen dimensions where possible
- Do not let login screen persist in navigation history after home page loads

---

### Task 1: Onboarding Screen Layout Scaling

**Files:**
- Modify: `lib/screens/onboarding_screen.dart`

- [ ] **Step 1: Replace fixed height ratio with Expanded PageView**
  Remove the `SizedBox(height: MediaQuery.of(context).size.height * 0.55)` wrapping the `PageView.builder` in [onboarding_screen.dart](file:///mnt/d/Projects/emosync_app/lib/screens/onboarding_screen.dart#L92-L106) and replace it with `Expanded(child: PageView.builder(...))`. Remove the extra `const Spacer(flex: 1)` spacers so that the `PageView` takes up all available vertical space naturally.

- [ ] **Step 2: Cap illustration height and update scroll physics**
  In [onboarding_screen.dart](file:///mnt/d/Projects/emosync_app/lib/screens/onboarding_screen.dart#L193-L514), update `_buildQuadrantPage`, `_buildActivitiesPage`, and `_buildStatsPage` to compute a dynamic illustration size. Set:
  ```dart
  double size = MediaQuery.of(context).size.width - 64;
  double maxIllustrationHeight = MediaQuery.of(context).size.height * 0.28;
  if (size > maxIllustrationHeight) {
    size = maxIllustrationHeight;
  }
  ```
  And apply this size to the width/height of the illustration container. Also change `physics: const NeverScrollableScrollPhysics()` in the `SingleChildScrollView` to `physics: const BouncingScrollPhysics()`.

- [ ] **Step 3: Verification**
  Ensure code compiles successfully.
  Run: `flutter analyze`
  Expected: No errors in `onboarding_screen.dart`.

- [ ] **Step 4: Commit**
  ```bash
  git add lib/screens/onboarding_screen.dart
  git commit -m "fix(onboarding): make onboarding layout responsive and scrollable on small screens"
  ```

---

### Task 2: Top Logo Safe Area Implementation

**Files:**
- Modify: `lib/screens/home_screen.dart`
- Modify: `lib/screens/journal_screen.dart`
- Modify: `lib/screens/content_screen.dart`
- Modify: `lib/screens/friend_screen.dart`
- Modify: `lib/screens/profile_screen.dart`

- [ ] **Step 1: Update headers in Home, Journal, Content, Friend, and Profile Pages**
  In the following files, modify `_buildHeader()` to wrap its `Container` in a `SafeArea(bottom: false, child: ...)` widget:
  - [home_screen.dart](file:///mnt/d/Projects/emosync_app/lib/screens/home_screen.dart)
  - [journal_screen.dart](file:///mnt/d/Projects/emosync_app/lib/screens/journal_screen.dart)
  - [content_screen.dart](file:///mnt/d/Projects/emosync_app/lib/screens/content_screen.dart)
  - [friend_screen.dart](file:///mnt/d/Projects/emosync_app/lib/screens/friend_screen.dart)
  - [profile_screen.dart](file:///mnt/d/Projects/emosync_app/lib/screens/profile_screen.dart)

- [ ] **Step 2: Clean up outer SafeArea on Profile page**
  In [profile_screen.dart](file:///mnt/d/Projects/emosync_app/lib/screens/profile_screen.dart#L312), replace the outer `SafeArea` wrapping the `SingleChildScrollView` with just the `SingleChildScrollView` since safety is now handled directly by the header and interior paddings.

- [ ] **Step 3: Verification**
  Run: `flutter analyze`
  Expected: No errors in modified screens.

- [ ] **Step 4: Commit**
  ```bash
  git add lib/screens/home_screen.dart lib/screens/journal_screen.dart lib/screens/content_screen.dart lib/screens/friend_screen.dart lib/screens/profile_screen.dart
  git commit -m "fix(ui): wrap all top headers in SafeArea to prevent overlap with status bar"
  ```

---

### Task 3: Navigation Flow and Back Button fixes

**Files:**
- Modify: `lib/screens/login_screen.dart`
- Modify: `lib/screens/register_screen.dart`

- [ ] **Step 1: Update login success navigation**
  In [login_screen.dart](file:///mnt/d/Projects/emosync_app/lib/screens/login_screen.dart#L53-L58), change `Navigator.pushReplacement` to:
  ```dart
  Navigator.pushAndRemoveUntil(
    context,
    MaterialPageRoute(builder: (context) => const HomePage()),
    (route) => false,
  );
  ```

- [ ] **Step 2: Update register success navigation**
  In [register_screen.dart](file:///mnt/d/Projects/emosync_app/lib/screens/register_screen.dart#L174-L177), change `Navigator.pushReplacement` to:
  ```dart
  Navigator.pop(context);
  ```

- [ ] **Step 3: Verification**
  Run: `flutter analyze`
  Expected: Code compiles and static analysis passes.

- [ ] **Step 4: Commit**
  ```bash
  git add lib/screens/login_screen.dart lib/screens/register_screen.dart
  git commit -m "fix(nav): prevent back button from logging out by clearing stack on login and popping on register"
  ```

---

### Task 4: Streak and Dominant Mood Alignment

**Files:**
- Modify: `lib/screens/home_screen.dart`

- [ ] **Step 1: Standardise Streak Card content and styling**
  In [home_screen.dart](file:///mnt/d/Projects/emosync_app/lib/screens/home_screen.dart#L650-L697), update `_buildStreakCard()` to:
  - Add a Row header at the top with a fire icon (size 20) and a "STREAK HARIAN" label (fontSize 10).
  - Use padding of `16` all around.
  - Set the value font size to `24`.
  - Set bottom description font size to `10`.

- [ ] **Step 2: Standardise Mood Dominant Card content and styling**
  In [home_screen.dart](file:///mnt/d/Projects/emosync_app/lib/screens/home_screen.dart#L699-L761), update `_buildMoodDominantCard()` to:
  - Add a Row header at the top with `Icons.psychology` icon (size 20) and "MOOD DOMINAN" label (fontSize 10).
  - Use padding of `16` all around.
  - Set the value font size to `24`.
  - Set bottom description font size to `10`.

- [ ] **Step 3: Wrap dashboard cards row in IntrinsicHeight**
  In [home_screen.dart](file:///mnt/d/Projects/emosync_app/lib/screens/home_screen.dart#L297-L303), wrap the row in `IntrinsicHeight` and configure `CrossAxisAlignment.stretch`:
  ```dart
  IntrinsicHeight(
    child: Row(
      crossAxisAlignment: CrossAxisAlignment.stretch,
      children: [
        Expanded(child: _buildStreakCard()),
        const SizedBox(width: 16),
        Expanded(child: _buildMoodDominantCard()),
      ],
    ),
  )
  ```

- [ ] **Step 4: Verification**
  Run: `flutter analyze`
  Expected: Success.

- [ ] **Step 5: Commit**
  ```bash
  git add lib/screens/home_screen.dart
  git commit -m "fix(dashboard): align streak and mood cards structure and ensure equal height"
  ```

---

### Task 5: App Name Updates

**Files:**
- Modify: `android/app/src/main/AndroidManifest.xml`
- Modify: `ios/Runner/Info.plist`

- [ ] **Step 1: Update Android label**
  Change `android:label="emosync_app"` in [AndroidManifest.xml](file:///mnt/d/Projects/emosync_app/android/app/src/main/AndroidManifest.xml#L4) to `android:label="EmoSync"`.

- [ ] **Step 2: Update iOS display name and bundle name**
  In [Info.plist](file:///mnt/d/Projects/emosync_app/ios/Runner/Info.plist#L9-L18), change the string value under `CFBundleDisplayName` from `Emosync App` to `EmoSync`, and `CFBundleName` from `emosync_app` to `EmoSync`.

- [ ] **Step 3: Commit**
  ```bash
  git add android/app/src/main/AndroidManifest.xml ios/Runner/Info.plist
  git commit -m "chore(config): rename installed app to EmoSync on Android and iOS"
  ```

---

### Task 6: Custom Launcher Icon Integration

**Files:**
- Modify: `pubspec.yaml`
- Create: `assets/icon/app_icon.png`

- [ ] **Step 1: Add dependencies to pubspec.yaml**
  Add `flutter_launcher_icons: ^0.13.1` under `dev_dependencies` in [pubspec.yaml](file:///mnt/d/Projects/emosync_app/pubspec.yaml#L29-L33) and write the configuration block:
  ```yaml
  flutter_launcher_icons:
    android: "launcher_icon"
    ios: true
    image_path: "assets/icon/app_icon.png"
    adaptive_icon_background: "#FAF9F6"
    adaptive_icon_foreground: "assets/icon/app_icon.png"
  ```

- [ ] **Step 2: Copy launcher icon image**
  Copy the generated launcher icon image from `/home/user/.gemini/antigravity-cli/brain/6015a84b-2bf9-45bb-a982-04398f0b4cc3/app_icon_1783142646462.jpg` to the project directory `/mnt/d/Projects/emosync_app/assets/icon/app_icon.png` (creating the directories if needed). Note: Convert/rename to `.png` format.

- [ ] **Step 3: Run flutter pub get and flutter_launcher_icons**
  Propose command to run:
  `flutter pub get && flutter pub run flutter_launcher_icons`
  Expected: Launcher icons generated successfully for Android and iOS.

- [ ] **Step 4: Commit**
  ```bash
  git add pubspec.yaml assets/icon/app_icon.png
  git commit -m "chore(icon): add custom launcher icons based on splash screen blobs"
  ```
